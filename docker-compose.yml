version: '3.8'

services:
  fridge-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fridge-dashboard
    privileged: true
    ports:
      - "8050:8050"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
